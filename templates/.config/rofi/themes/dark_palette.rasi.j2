* {
    grey:       {{ base.grey }};
    {% for c in base.dark|color_cycle(base.grey, 5) %}
    bkgrd{{ loop.index0 }}:     {{ c }};
    {%- endfor %}
    {% for c in base.dark|color_cycle(base.grey, 5) %}
    bkgrd{{ loop.index0 }}aa:   {{ c }}e0;
    {%- endfor %}

    background: {{ base.dark }};
    foreground: {{ base.white }};
    accent: {{ base.primary }};
    xaccent: {{ base.primary.l.l }};

    {% for c_name, c_val in colors.items() %}
    {{ "{:10s}".format(c_name) }}: {{ c_val }};
    {%- endfor %}

    {% for c_name, c_val in colors.items() %}
    x{{ "{:9s}".format(c_name) }}: {{ c_val.l.l }};
    {%- endfor %}
}
// vim: ft=css
